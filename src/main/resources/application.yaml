spring:
  application:
    name: z-compliance-review-service

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    root: INFO
    com.shawn.compliance: DEBUG